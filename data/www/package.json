{
    "name": "slascicarna_spletna_stran",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "sass scss:css --no-source-map",
        "dev": "sass --watch scss:css --no-source-map"
    },
    "dependencies": {
        "bootstrap": "^5.3.0"
    },
    "devDependencies": {
        "sass": "^1.69.0"
    }
}
